import { render, screen } from "@testing-library/react";
import Home from "./Home";

it("mock api call", async () => {
//   fetch.mockResponse(
//     JSON.stringify({ capital: "DC", name: "USA", region: "NA", population: 5 })
//   );
//   render(<Home />);
//   screen.debug();
});
